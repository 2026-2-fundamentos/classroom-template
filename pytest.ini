[pytest]
# Solo se buscan pruebas en las carpetas tests/ de cada actividad. Así el
# código del estudiante en src/ no se importa durante el descubrimiento,
# aunque el editor agregue opciones como --doctest-modules.
testpaths =
    PRE_*/tests
    LAB_*/tests
